// Enumerado da env do NODE_ENV
export enum NodeEnv {
  PRODUCTION = 'production',
  DEVELOPMENT = 'development',
}

// Opções de ordenação da paginação
export enum OrderPaginate {
  ASC = 'ASC',
  DESC = 'DESC',
}

// Tipos de token
export enum TokenType {
  BEARER = 'Bearer',
}
